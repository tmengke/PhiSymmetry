bool skipHEChannel(int iphi,int ieta) {
  if (
      //      (iphi>=55 && iphi<=56 && ieta<=-16)
      //(iphi>=55 && iphi<=58 && ieta<=-16) // also something...
      )
    return true;
  else return false;
}
